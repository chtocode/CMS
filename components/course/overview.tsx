import React from 'react';
import { Course } from '../../lib/model/course';

export type CourseOverviewProps = Course;

export default function CourseOverview(props: CourseOverviewProps) {
  const {} = props;
  return <></>;
}
