export enum CourseStatusText {
  'finished',
  'processing',
  'pending',
}

export enum CourseStatusBadge {
  'warning',
  'success',
  'default',
}

export enum CourseStatusColor {
  'default',
  'green',
  'orange',
}
